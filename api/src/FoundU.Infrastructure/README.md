# FoundU.Infrastructure

EF Core DbContext, migrations, external service adapters (file storage, notifications,
AI HTTP client). Depends on Application and Domain.

Step 2 (DbContext + migration) and Step 3 (Identity) land here.
